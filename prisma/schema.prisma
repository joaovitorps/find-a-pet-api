generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Org {
  id        String @id @default(uuid())
  email     String @unique
  password  String
  ownerName String
  name      String
  phone     String
  address   String
}
